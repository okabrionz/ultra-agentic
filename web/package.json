{
  "name": "web",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "test": "bun test ./src",
    "test:e2e": "node node_modules/@playwright/test/cli.js test",
    "test:dist": "bun test ./tests/build-output.smoke.ts",
    "verify": "bun run test && bun run astro check && bun run build && bun run test:dist && bun run test:e2e"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.2",
    "astro": "^7.0.7",
    "tailwindcss": "^4.3.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.9",
    "@playwright/test": "^1.61.1",
    "@types/bun": "^1.3.14",
    "typescript": "^6.0.0"
  }
}